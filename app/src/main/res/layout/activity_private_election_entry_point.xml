<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.PrivateElectionEntryPoint">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbarpriv"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:layout_constraintTop_toTopOf="parent"
        app:title="Private Elections"
        app:titleTextAppearance="@style/azo" />

    <TextView
        android:id="@+id/textView8"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Private elections enable organizations/institutions to conduct targeted polls for dpecific purposes.\n\nPre-register yourself using the key provided by the creator. Your credentials will be verified before voting session begins.\nVerified individuals can vote using an OTP that will be sent to their phone once the session begins."
        android:padding="10dp"
        app:layout_constraintTop_toBottomOf="@id/toolbarpriv" />

    <Button
        android:id="@+id/register"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/buttonbg"
        android:text="Register for Upcoming Election"
        android:textAllCaps="false"
        android:layout_marginTop="30dp"
        android:theme="@style/Widget.MaterialComponents.Button.OutlinedButton"
        app:backgroundTint="@color/button"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textView8" />

    <Button
        android:id="@+id/vote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:background="@drawable/buttonbg"
        android:text="Vote in Ongoing Election"
        android:textAllCaps="false"
        android:theme="@style/Widget.MaterialComponents.Button.OutlinedButton"
        app:backgroundTint="@color/button"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/register" />

    <ImageView
        android:layout_width="180dp"
        android:layout_height="100dp"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:id="@+id/privBg"/>

</android.support.constraint.ConstraintLayout>